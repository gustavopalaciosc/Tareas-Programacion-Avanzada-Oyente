 """
        try:
            mi_tributo = int(input("\nEliga uno utilizando el número respectivo del tributo a seleccionar: "))

            if mi_tributo == 0:

                print("\nDEBES INGRESAR UN NUMERO ENTRE 1 Y 12!\n")
                self.elegir_tributo() 
            
            else:
                file = open("tributos.csv")
                a = file.readlines()
                return a[mi_tributo].split(",")[0]

        except (IndexError, ValueError):
            print("\nDEBES INGRESAR UN NUMERO ENTRE 1 Y 12!\n")
            self.elegir_tributo()
        """

"""
class persona():
    
    def __init__(self, energia, fuerza):

        self.energia = energia
        self.fuerza = fuerza 
        self.objetos = [Consumible("manzana", "consumible", 10)]

    def usar_objeto(self):

         return self.objetos[0].entrega_beneficio(self)
"""        